{
  "name": "mimo2api-worker",
  "version": "1.0.0",
  "description": "mimo2api Cloudflare Worker 版 - WebSocket 隧道网关",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "cf-typegen": "wrangler types",
    "bundle": "esbuild src/index.ts --bundle --outfile=worker.js --format=esm --target=es2022 --external:cloudflare:workers --sourcemap"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260609.1",
    "typescript": "^5.7.0",
    "wrangler": "4.86.0"
  }
}
